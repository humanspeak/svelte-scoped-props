# Implementation Plans — docs-kit-parity

Generated by the improve skill on 2026-07-23. Goal: bring this repo's docs
site to feature parity with sibling `*.svelte.page` repos after the docs-kit
`2026.6.9 → 2026.7.6` bump (landed at commit `6c77914` without feature
wiring). Reference consumer: `humanspeak/svelte-markdown`.

## Execution order & status

| Plan | Title                                             | Priority | Effort | Depends on | Status |
| ---- | ------------------------------------------------- | -------- | ------ | ---------- | ------ |
| 001  | Adopt docs-kit 2026.7.6 features for parity       | P2       | S/M    | —          | DONE (guard PASS at 42abd88; IndexNow + PagerV2 + verify-only items; exampleMirrors deferred → 002) |
| 002  | Restructure example pages to sections pattern     | P3       | M      | 001        | DONE (guard PASS at e7efd2c; in-place Path A after investigation disproved per-slug premise; wrapper/map retired per rev2; full 2026.7.6 parity reached) |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- Single-plan batch. The docs-kit version bump itself is already committed
  (`6c77914` on `chore/package-updates`); this plan is pure adoption.
- IndexNow key generated for this site: `34c0a202-56db-44a1-8776-146bc8ff8e99`
  (public by protocol design; do not reuse in other repos).

## Findings considered and rejected

- `hideNpm` / `hideLogo` header config: for sites without an npm package or
  logo (e.g. redactsensitiveinfo-com). N/A here — this repo publishes a
  package and shows its brand.
- Ecosystem README updater: already adopted org-wide via
  `.github/workflows/npm-publish.yml` (fetches the script from docs-kit main
  at publish time). Verify-only in plan 001.
- Bugfix commits in the delta (llms.txt separator, header compact brand,
  pager hover): automatic with the version bump; nothing to wire.
- `sitemapManifestPlugin` `extraPages` (seen in svelte-markdown): serves their
  compare-pages feature, not part of this delta — not parity-relevant.
