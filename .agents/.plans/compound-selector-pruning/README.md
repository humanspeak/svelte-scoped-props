# Implementation Plans — compound-selector-pruning

Generated by the improve skill on 2026-07-23, from the field report
`HANDOFF-compound-selector-pruning.md` (repo root). Each executor: read the plan
fully before starting, honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title                                                                | Priority | Effort | Depends on | Status |
| ---- | -------------------------------------------------------------------- | -------- | ------ | ---------- | ------ |
| 001  | Keep compound selectors of scoped classes from being pruned           | P1       | S      | —          | DONE (guard PASS at 74ecd0e, branch advisor/001-compound-scoped-classes) |
| 002  | Synthesize marker structure per selector (typed + combinator support) | P2       | M      | 001        | DONE (guard PASS at 9e266e5 after post-review fix; branch advisor/002-structural-marker-synthesis) |
| 003  | Document structural marker behavior + compound-selector example       | P2       | M      | 002        | DONE (guard PASS at bcfb645, branch advisor/003-docs-and-example) |
| 004  | Mask CSS comments; render marker nodes as svelte:element              | P1       | M      | 002, 003   | DONE (guard PASS at 14501d7; fixes PR #12 adversarial-review findings + content-model errors) |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- 002 requires 001 because it reuses 001's `mapCssPreludes` / `stripQuotedSections`
  helpers and its prelude-scoped class collection; 001 also fixes the class-regex
  bug that would otherwise starve 002's chains of their class names.
- 003 requires 002 because it documents 002's structural marker and its example
  page compiles against the fixed preprocessor (docs workspace consumes
  `workspace:*`); executed on a branch stacked on advisor/002.
- Changesets were considered for 003 and dropped: the repo has no `.changeset/`
  directory or changesets tooling (the trunk.yaml reference is boilerplate);
  release-notes process is a separate concern.

## Open findings (surfaced during execution, not yet planned)

- `DEFAULT_RUNTIME_MODULE` (src/index.ts) and README's install section use the
  unscoped name `svelte-scoped-props`, but the package publishes as
  `@humanspeak/svelte-scoped-props`; the docs site works around it by passing
  `runtimeModule` explicitly. Coupled code+docs fix — candidate future plan
  (number not yet assigned; 004 was taken by the PR-review fixes).
- Docs lint gate is red repo-wide: prettier flags `docs/src/worker-configuration.d.ts`
  and a vendored skill file; the scoped-props ESLint processor raises
  `prefer-const` on every `let x = $derived(...)` demo — possibly a
  rune-awareness bug in the processor worth investigating. Candidate plan 005.
- `docs` script `examples-catalog:sync` is dead — it scans `examples/+page.ts`
  for a catalog object that was refactored out; the example registry is now
  `$lib/examplesIndex.ts` (docs-kit-parity plan 001).
- Cross-batch note (per plan 003's maintenance note): `demoCodeDependencies`
  and `demo-code-samples.ts` were retired by docs-kit-parity plan 002 (rev2) —
  pages now list `demoCodeSample` panels inline from `$lib/demo-loaders`. Plan
  003's registration step for new examples is superseded accordingly.

## Findings considered and rejected / deferred

- **Handoff "Suggested fix 1" (co-locate marker classes per directive)**: already
  the implemented design — `addCssMarkerSnippet` renders one div with all
  collected classes. The actual root cause is the class-regex lookbehind; plan
  001 fixes that instead.
- **Handoff "Suggested fix 3" (document the limitation)**: superseded — plan 001
  removes the limitation for same-element compounds. Remaining limitations
  (element-qualified compounds, descendant combinators) are listed in plan 001's
  Maintenance notes for a future docs pass.
- **Handoff note "runtimeModule looks like its default and could be dropped from
  the quickstart"**: inverted — the built-in default is `svelte-scoped-props/runtime`
  (src/index.ts:59), which does NOT resolve for the published
  `@humanspeak/svelte-scoped-props` package, so the quickstart's explicit option
  is load-bearing. The right fix is changing the default constant; deferred as a
  separate small plan if wanted.
- **E2e fixture case for compound selectors**: deferred to keep plan 001 small;
  noted in its Maintenance notes.
