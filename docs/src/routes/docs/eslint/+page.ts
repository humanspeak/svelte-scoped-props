import type { PageLoad } from './$types'

export const load: PageLoad = () => ({
    title: 'ESLint',
    description: 'Configure the Svelte Scoped Props ESLint processor for svelte/valid-compile.'
})
