import type { PageLoad } from './$types'

export const load: PageLoad = () => ({
    title: 'Get Started',
    description:
        'Install and configure Svelte Scoped Props for explicit parent-scoped component class props.'
})
