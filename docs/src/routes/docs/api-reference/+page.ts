import type { PageLoad } from './$types'

export const load: PageLoad = () => ({
    title: 'API Reference',
    description:
        'Reference for Svelte Scoped Props exports, options, transform output, runtime helper, and diagnostics.'
})
