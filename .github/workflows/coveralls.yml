name: Coveralls

permissions:
  contents: read

on:
  workflow_dispatch:

jobs:
  not-yet-wired:
    runs-on: blacksmith-2vcpu-ubuntu-2404 # trunk-ignore(actionlint/runner-label)
    steps:
      - name: Coverage placeholder
        run: echo "Coverage upload is intentionally not wired for the alpha package yet."
